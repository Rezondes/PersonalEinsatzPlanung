/** ChangelogView-specific copy. The page heading itself reuses nav.changelog (identical text). */
const changelog = {
  loading: 'Änderungen werden geladen…',
  empty: 'Noch keine Einträge vorhanden.',
  viewOnGitHub: 'Auf GitHub ansehen',
  opensInNewWindow: '(öffnet in neuem Fenster)',
} as const;

export default changelog;
